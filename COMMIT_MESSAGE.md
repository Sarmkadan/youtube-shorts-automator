Review and improve ONE existing file as a senior C# engineer: `src/Exceptions/UploadExceptionJsonExtensions.cs`. Fix everything on this checklist, editing the file in place: 1. FAKED LOGIC: any method that hardcodes its result, ignores its input, or carries a comment like "simplified"/"for demo" - either implement it for real or delete the method entirely. 2. Missing guard clauses: add ArgumentNullException.ThrowIfNull / ArgumentException.ThrowIfNullOrEmpty where inputs are dereferenced. 3. Correctness bugs: culture-sensitive parsing/formatting, swallowed exceptions, off-by-one, wrong null-handling. 4. Non-idiomatic C#: replace if-chains with pattern matching, use expression bodies for one-liners, seal what can be sealed. 5. XML docs: ensure every public member has an accurate <summary> and <exception> tags for every throw. If the file is already clean, make at least the smallest genuine improvement you can find (naming, doc precision, a missing edge case). Do NOT change the public API surface (names/signatures of existing public members) - callers may exist. HARD RULES (build check runs after you; violating any fails the task): - Do NOT touch .csproj/.sln/.slnx/Directory.Build.props or any other existing file except the one named above. - Do NOT write tests. Do NOT add NuGet packages. No mention of AI/assistant in code or commits. - The whole solution MUST still compile with `dotnet build`. - Commit message: conventional commits style.